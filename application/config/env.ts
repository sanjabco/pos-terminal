// Environment configuration for POS system
export const ENV = {
    // Helper function to check if using Sepehr POS
    isSepehrPOS: () => true, // Default to Sepehr POS

    // Helper function to check if using default POS
    isDefaultPOS: () => false,
};

export default ENV; 